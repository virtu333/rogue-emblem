# Default: play offline and save progress on this device.
# Enable only for a deliberately cloud-enabled build. Credentials alone do not enable sync.
VITE_CLOUD_ENABLED=false
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_DEBUG_MODE=
